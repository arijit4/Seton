package com.arijit4.nothing.notes.viewmodels

import androidx.lifecycle.ViewModel
import com.arijit4.nothing.notes.db.NoteEvent
import kotlinx.coroutines.flow.MutableStateFlow
import kotlinx.coroutines.flow.asStateFlow

class HomeViewModel: ViewModel() {
//    val x by noteDao.getAllNotes().collectAsState(initial = emptyList())
}

annotation class HomeState
